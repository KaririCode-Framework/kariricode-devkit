<?xml version="1.0"?>
<ruleset name="KaririCode Parser">
    <description>Coding standard with exceptions for PHP keyword conflicts</description>

    <!-- Alvos -->
    <file>src</file>
    <file>tests</file>

    <!-- Ignorar -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>var/*</exclude-pattern>
    <exclude-pattern>build/*</exclude-pattern>
    <exclude-pattern>coverage/*</exclude-pattern>
    <exclude-pattern>public/index.php</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>

    <!-- Exceptions -->
    <arg name="colors" />
    <arg name="parallel" value="8" />
    <arg name="extensions" value="php" />
    <!-- PHP 8.4 -->
    <config name="php_version" value="80400" />
    <rule ref="PSR12">
        <exclude name="Squiz.Classes.ValidClassName.NotCamelCaps" />
    </rule>

    <!-- Exception for classes with trailing underscore -->
    <rule ref="PSR1.Classes.ClassDeclaration">
        <exclude name="PSR1.Classes.ClassDeclaration.MissingNamespace" />
        <exclude name="PSR1.Classes.ClassDeclaration.TrailingUnderscore" />
        <exclude name="Squiz.Classes.ValidClassName.NotPascalCase" />
    </rule>

    <rule ref="PSR2">
        <exclude name="Squiz.Classes.ValidClassName.NotCamelCaps" />
        <exclude name="PSR2.ControlStructures.SwitchDeclaration.BodyOnNextLineCASE" />
    </rule>

    <!-- Allow longer lines for complex types -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120" />
            <property name="absoluteLineLimit" value="180" />
        </properties>
    </rule>

    <arg value="ps" />
    <arg name="colors" />
    <arg name="parallel" value="8" />
</ruleset>
